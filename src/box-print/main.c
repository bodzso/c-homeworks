#include <stdio.h>

int main(void)
{
    int n;
    scanf("%d",&n);
    int i;
    for(i = 0; i < (n+2)*(n+2); i++ ){
        printf("%s",((i == 0) || (i == n+1) || i == (n+2)*(n+2)-(n+2) || i == (n+2)*(n+2)-1) ? (i == n+1) ? "+\n"  : "+" : (i < n+2  || i > (n+2)*(n+2)-(n+2)) ? "-" : ((i % (n+2)) == 0) ? "|" : (((i+1) % (n+2)) == 0) ? "|\n" : (i % (n+3) == 0) ? (i == ((n+2)*(n+2) /2) ) ? "X" : "\\" : (i % (n+1) == 0) ? "/" : " ");
    }
    return 0;
}
